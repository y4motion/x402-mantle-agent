[package]
name = "ipfs-router"
version = "0.1.0"
edition = "2024"

[dependencies]
core-ipc = { version = "0.1.0", path = "../core-ipc" }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
tokio = { version = "1.0", features = ["full"] }
